# Node
/node_modules
npm-debug.log
yarn-error.log

# IDEs and editors
.idea/
*.sublime-workspace

# Environment
.env
.env.*

# Logs
*.log

# Dev-only files
dev-server.py
feedback.json

# Agent working memory (not part of the website)
CLAUDE.md
.claude/
.claude/skills/
memory/

# Architect Layer-2 session tooling (local-only, never published)
.claude/architect-tools/
.claude/architect-sweep/
.claude/hooks/
.claude/agents/
.claude/scheduled_tasks.lock
.claude/.tmp/
# Seed-reset snapshots relocated out of the prototype root (reset-seed-for-test.sh)
.claude/seed-reset-snapshots/

# Audio transcript intermediates (HTML → transcript → audio pipeline)
*.transcript.md

# Local-only blog drafts (drafts stay local until published)
# blog/05-*, blog/06-*, blog/07-*, blog/08-* — Part-2 drafts in active review, tracked so user can see diffs
blog/09-*
blog/draft-*


# System files
.DS_Store
Thumbs.db

# Python bytecode (from tools/ scripts)
__pycache__/
*.pyc
*.pyo

# Local diagram-review comments (dev tool, not deployed)
diagram-comments.json

# Free-TTS DRAFT audio — local review only, regeneratable via tools/generate_free_audio.py; paid finals get committed
*.draft.mp3
*.draft.mp3.meta.json

# YouTube video-content voice recordings (personal, local-only — archived by architect-tools/youtube-capture.sh)
.claude/youtube-video-content/

# Cloned-voice per-paragraph CHUNKS — internal working files (chunk-NNN.mp3/.txt,
# manifest.tsv, _pause.mp3) for per-paragraph re-record/replace. NEVER pushed.
# Only the COMBINED <slug>.mp3 (a file directly in audio/, one level up) is committed.
blog/b*/audio/*/

# Video production pipeline (Remotion + node_modules + per-project git) — LOCAL ONLY,
# never deployed. Final rendered videos are exported to assets/video/ (committed).
/video-projects/
