# Node
/node_modules
npm-debug.log
yarn-error.log

# IDEs and editors
.idea/
*.sublime-workspace

# Environment
.env
.env.*

# Logs
*.log

# Dev-only files
dev-server.py
feedback.json

# Agent working memory (not part of the website)
CLAUDE.md
.claude/skills/
memory/

# Architect Layer-2 session tooling (local-only, never published)
.claude/architect-tools/
.claude/architect-sweep/
.claude/hooks/
.claude/scheduled_tasks.lock
.claude/.tmp/
# Seed-reset snapshots relocated out of the prototype root (reset-seed-for-test.sh)
.claude/seed-reset-snapshots/

# Audio transcript intermediates (HTML → transcript → audio pipeline)
*.transcript.md

# Local-only blog drafts (drafts stay local until published)
# blog/05-*, blog/06-*, blog/07-*, blog/08-* — Part-2 drafts in active review, tracked so user can see diffs
blog/09-*
blog/draft-*


# System files
.DS_Store
Thumbs.db

# Python bytecode (from tools/ scripts)
__pycache__/
*.pyc
*.pyo

# Local diagram-review comments (dev tool, not deployed)
diagram-comments.json
