# CLAUDE.md — Blog 8 Mini-Series (From Apprentice to Architect)
**Version:** v0.1.0
**Series:** B8 — Pt 4 of the Part-2 (How) arc of the Hadosh Academy series on agent architecture

Per-series working memory for the B8 essays (`08_1..08_9`). Populated as work happens.

Ref-tag review job 1781190945888385572 (cycle 1) surveyed B8: IRR-4 (08_3 stale dated counts) +
IRR-5 (08_3 misattributed principles.md pointer) were deferred to pending job 1781200035845568970;
condensed — durable record in `.claude/knowledge/ref-tag-review/`.

---Ob---

---Pl---

## PLAN scope-declaration — ref-tag stale-count fixes (job 1782320396187605322, cycle 1)

**This dir (`blog/b8/`) is activated for EXECUTE.** b8 is LINE-CLEAN (0 line-number ref-tags) — its
only work is the two `08_3` stale-count findings. Edit BOTH `.md` AND `.html` — mirror invariant.

- **#2 — `08_3` `knowledge-layer-active-and-archived-silos`:** ref-tag (and the adjacent body prose,
  "roughly 258k") bakes a dated "258,238 words / 228 files / 21 silos" + an enumerated silo count →
  reduce to a stable shape + name the live `find` command as the authority. Drop the dated specifics.
- **#3 — `08_3` `memory-fifty-entries-feedback-heavy`:** slug encodes "fifty"; content says "55
  entries" (dated) → stop baking a count in the slug + drop the dated number; keep a stable slug +
  value-bearing summary.
- **Parallel-session note:** a Layer-2 session may touch the PLUGIN layer, not blog files — no
  collision on 08_3 blog edits (user ruling 2026-06-24). Still `git status`-check before committing.
- **Re-registered this PLAN pass (post-clear resume):** the altered list reset to the job dir at the
  `/clear` boundary, so this dir is re-touched HERE to re-declare `blog/b8/` editable for EXECUTE;
  the two `08_3` stale-count fixes stand and EXECUTE re-confirms each baked numeral in-file at
  fix-time before stripping it.

---Ex---

---Ve---
